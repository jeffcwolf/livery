[package]
name = "playfair-design"
version = "0.1.0"
edition = "2021"
description = "Vendored snapshot of Playfair design system for stub builds"

[dependencies]
iced = { version = "0.14", features = ["canvas"] }
serde = { version = "1", features = ["derive"] }
