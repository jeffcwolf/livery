[package]
name = "vitrine"
version = "0.1.0"
edition = "2021"
description = "Stub for vitrine crate used in CI/environments without the real vitrine workspace"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
raw-window-handle = "0.6"
